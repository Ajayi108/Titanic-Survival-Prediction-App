// import { test, expect } from "@playwright/test";

// test("user can log in successfully", async ({ page }) => {
//   await page.goto("/login");

//   await page.fill('input[type="email"]', "hamza@ai.com");
//   await page.fill('input[type="password"]', "secret");

//   await page.click('button[type="submit"]');

//   await expect(page).toHaveURL(/(\/|\/calculator)/i);

//   // Check that survival calculator or navbar is visible
//   await expect(page.locator("text=Prediction Calculator")).toBeVisible();
// });
